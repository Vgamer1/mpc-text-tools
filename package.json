{
  "name": "mcp-workshop",
  "version": "1.1.0",
  "description": "MCP server: dev tools for AI agents — text processing, GitHub intel, log analysis. Hosted on Cloudflare Workers.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "diff": "^8.0.4",
    "tokenx": "^1.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.0.0"
  }
}